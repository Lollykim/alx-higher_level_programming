#!/usr/bin/python3
def new_in_list(my_list, idx, element):
cp = a[:]
for i in range(len(my_list)):

      if idx < 0:
     return a[:]

if idx >= len(my_list):
return a[:]
else:
a[:][idx] = element
return a[:]
